apiVersion: 1
datasources:
  - name: k6
    type: influxdb
    access: proxy
    database: k6
    user: $INFLUXDB_ADMIN_USER
    password: $INFLUXDB_ADMIN_PASSWORD
    url: http://influxdb:8086
    isDefault: true
    editable: true
